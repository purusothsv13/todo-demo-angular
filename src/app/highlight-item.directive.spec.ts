import { HighlightItemDirective } from './highlight-item.directive';

describe('HighlightItemDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightItemDirective();
    expect(directive).toBeTruthy();
  });
});
